'use client';
import React from 'react';
import styles from './Menu.module.css';

const HomeIndicator: React.FC = () => {
  return <div className={styles.homeIndicator} aria-hidden="true" />;
};

export default HomeIndicator;
